[package]
name = "redox"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.6.2", features = ["runtime-tokio-native-tls", "sqlite"]}
tokio = { version = "1.20.0", features = ["macros"]}
termion = "2.0.1"
clipboard = "0.5.0"
regex = "1.8.3"